After using npm to install ws dependencies normally, delete this "ws": "./utils/browser/WebSocket" in node_modules/puppeteer/package.json and it will run normally


Job Pagination.



DELETE FROM `wpis_jobs` WHERE created_at < DATE_SUB(NOW(), INTERVAL 1 MONTH) ORDER BY `created_at` DESC